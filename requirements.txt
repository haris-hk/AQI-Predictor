# Core pipeline. Pinned to minor versions so a scheduled run cannot break
# overnight on someone else's release.
pandas>=2.1,<3.1
numpy>=1.26,<3
scikit-learn>=1.4,<2
pyarrow>=15
requests>=2.31
joblib>=1.3
statsmodels>=0.14
shap>=0.44
matplotlib>=3.8
